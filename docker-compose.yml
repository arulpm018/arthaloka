# Deploy monolith — satu service untuk 2 orang.
#
#   1. cp .env.production.example .env.production  → isi secrets runtime
#   2. docker compose up -d --build
#   3. (opsional) reverse-proxy HTTPS → localhost:3000
#
# Build args NEXT_PUBLIC_* diambil otomatis dari file .env (compose membacanya).
services:
  arthafiloka:
    build:
      context: .
      args:
        NEXT_PUBLIC_FIREBASE_API_KEY: ${NEXT_PUBLIC_FIREBASE_API_KEY}
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
        NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
        NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
        NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
        NEXT_PUBLIC_FIREBASE_APP_ID: ${NEXT_PUBLIC_FIREBASE_APP_ID}
        NEXT_PUBLIC_ALLOWED_EMAILS: ${NEXT_PUBLIC_ALLOWED_EMAILS}
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    restart: unless-stopped
